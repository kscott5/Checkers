<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="karega.scott.checkers"
    android:versionCode="1"
    android:versionName="1.0" >

    <instrumentation android:targetPackage="karega.scott.checkers" android:name="android.test.InstrumentationTestRunner"></instrumentation>
    
    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme" android:description="@string/app_description">
        <activity android:name="karega.scott.checkers.MainActivity" android:label="@string/app_name" android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="karega.scott.checkers.BoardActivity" android:label="@string/title_activity_board" android:screenOrientation="fullSensor">
        </activity>
        <uses-library android:required="true" android:name="android.test.runner"/>
    </application>

</manifest>
