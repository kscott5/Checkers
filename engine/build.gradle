plugins {
   id  'java-library'
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(24))
    }
}

compileJava {
    options.release = 17
}

version = '1.2.1'

dependencies {
    testImplementation 'junit:junit:4.12'
}
